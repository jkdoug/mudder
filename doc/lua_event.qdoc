/*!
    \target lua events
    \page lua_event.html
    \startpage index.html Mudder Guide
    \title Event Handlers
    This is where crap about event handler functions in Lua will go.

    \list
        \li \c {Event.new(name, func)}
        \li \c {Event.raise(name, ...)}
        \li \c {Event.unregister(name, handler)}
    \endlist
 */
